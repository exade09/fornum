[package]
name = "fornum-fees"
version = "0.1.0"
edition = "2021"
description = "Per mint fee vaults for Fornum, claimable by whoever holds the number"
license = "MIT"

[lib]
crate-type = ["cdylib", "lib"]
name = "fornum_fees"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "1.2.0"
